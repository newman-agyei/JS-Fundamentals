// Get the first two arguments
const firstArgument = process.argv[2];
const secondArgument = process.argv[3];

// Print in the format "argument1 is argument2"
console.log(firstArgument + " is " + secondArgument);
